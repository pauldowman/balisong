#homepage
  #posts-and-about
    #about
      %h2 About this site 
      %p
        Welcome. This site is powered by the 
        = link_to "Balisong", "https://github.com/pauldowman/balisong"
        blogging app.
      %p
        Here is some info about Balisong:
      #readme
        !~ RDiscount.new(File.read("#{Rails.root}/README.md")).to_html

    #recent-posts
      %h2 Recent posts
      - @recent_posts.each do |p|
        .post-preview{:id => "page-#{p.id}"}
          %h3
            = link_to p.title, '/' + Page.urlify(p.id)
          %p
            = preview_page(p)
          .post-metadata
            &ndash;
            %time{:pubdate => true, :datetime => p.date.strftime("%Y-%m-%d")}
              = p.date.strftime("%B %e, %Y")
      %p
        More posts about:
        != @categories.sort.collect{ |c| link_to(c, "/category/#{c}")}.join(', ') + '.'

